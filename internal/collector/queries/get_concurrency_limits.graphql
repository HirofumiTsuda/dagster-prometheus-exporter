query GetConcurrencyLimits {
  instance {
    concurrencyLimits {
      concurrencyKey
      activeSlotCount
      assignedStepCount
      pendingStepCount
      limit
      usingDefaultLimit
    }
  }
}
