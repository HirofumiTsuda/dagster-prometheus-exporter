query GetDaemonHealth {
  instance {
    daemonHealth {
      allDaemonStatuses {
        daemonType
        required
        healthy
        lastHeartbeatTime
        lastHeartbeatErrors {
          message
        }
      }
    }
  }
}
